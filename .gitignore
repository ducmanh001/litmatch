# dependencies
node_modules/

# build outputs
dist/
tmp/
out-tsc/

# nx
.nx/cache
.nx/workspace-data

# env — không commit secret, dùng .env.example
.env
.env.local
.env.*.local

# chứng chỉ TLS tự ký cho dev LAN (test qua HTTPS trên thiết bị khác) — không commit private key
.dev-certs/

# test
coverage/

# IDE / OS
.idea/
.DS_Store

# logs
*.log

vite.config.*.timestamp*
vitest.config.*.timestamp*

# Next.js
.next
out

# TypeScript incremental build cache
*.tsbuildinfo
.dev-certs/*.key

# trạng thái và backup release local trên production host
deploy/production/.env
deploy/production/.release-state.json
deploy/production/backups/
