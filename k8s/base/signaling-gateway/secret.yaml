# Secret MANIFEST rỗng/placeholder — KHÔNG commit giá trị thật vào git.
# Điền qua sealed-secrets/Vault. JWT_SECRET PHẢI giống hệt giá trị dùng ở core-api/secret.yaml
# (gateway chỉ VERIFY access token, không bao giờ ký — docs/03 § 3.7, env.validation.ts comment).
apiVersion: v1
kind: Secret
metadata:
  name: signaling-gateway-secrets
  namespace: litmatch
  labels:
    app.kubernetes.io/name: signaling-gateway
    app.kubernetes.io/part-of: litmatch
  annotations:
    litmatch.dev/managed-by: 'điền qua sealed-secrets hoặc Vault, KHÔNG commit giá trị thật'
type: Opaque
stringData:
  JWT_SECRET: 'REPLACE_ME_SAME_AS_CORE_API'
  REDIS_URL: 'REPLACE_ME'
