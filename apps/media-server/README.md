# media-server

SFU (**LiveKit self-host — đã chốt**, xem `../../docs/03-architecture.md § 3.8.A`), sidecar không business logic, chỉ chuyển tiếp media. Chưa scaffold — xem `../../docs/07-roadmap.md` Giai đoạn 2.

Xem `CLAUDE.md` trong thư mục này trước khi thêm code.
