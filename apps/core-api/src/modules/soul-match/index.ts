/**
 * Public API của Soul Match module — module khác CHỈ import từ đây (arch test enforce).
 * Hiện chưa module nào cần gọi Soul Match → export tối thiểu.
 */
export { SoulMatchModule } from './soul-match.module';
