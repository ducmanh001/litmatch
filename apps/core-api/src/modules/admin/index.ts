/**
 * Public API của Admin module — module khác CHỈ được import từ file này,
 * không import thẳng file nội bộ (enforce bằng arch test, xem src/arch/).
 */
export { AdminModule } from './admin.module';
